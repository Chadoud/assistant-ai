# Local packaging only — copy to .env.release or run:
#   node scripts/sync-gmail-oauth-release-env.js
#
# Base64 of electron/resources/gmail_oauth_client.json (Desktop OAuth, redirect http://127.0.0.1:8789/callback).
# GitHub Actions: repository secret GMAIL_OAUTH_CLIENT_JSON_B64 (same value, one line, no quotes).
GMAIL_OAUTH_CLIENT_JSON_B64=
