global:
  resolve_timeout: 5m

route:
  receiver: blackhole
  group_by: ["alertname", "severity"]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h

receivers:
  # Replace with webhook_configs when ALERTMANAGER_WEBHOOK_URL is set on VPS.
  - name: blackhole
