# Optional — read-only MariaDB creds for datasuite (gitignored)
# Used by: npm run datasuite:generate-env
# Replace YOUR_IK_ID with your Infomaniak MariaDB user prefix.

DATASUITE_DB_USER=YOUR_IK_ID_datasuite
DATASUITE_DB_PASSWORD=
