import { SECONDARY_BTN_CLASS } from "../../../utils/styles";

/** Shared toolbar control sizing for grouping dropdown + rules popover. */
export const SORT_STRIP_TOOLBAR_BTN_CLASS = `${SECONDARY_BTN_CLASS} inline-flex items-center gap-1.5 text-xs py-1.5 px-2.5 min-h-[2rem]`;

/** Example preset chips under the flow canvas. */
export const SORT_STRIP_PRESET_CHIP_CLASS =
  "rounded-full border border-border bg-bg-secondary px-2.5 py-1 text-2xs text-text-secondary hover:border-accent/50 hover:text-text-primary transition-colors";
